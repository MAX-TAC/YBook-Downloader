# YBook Downloader

Android-приложение для поиска и сохранения электронных и аудиокниг, доступных пользователю в Яндекс Книгах.

> Используйте приложение только для контента, к которому у вашей учётной записи есть законный доступ. Проект не предназначен для обхода DRM или ограничений доступа. Проект не связан с Яндексом и использует внутренние недокументированные API Bookmate/Яндекс Книг, поэтому отдельные функции могут перестать работать после изменений на стороне сервиса.

## Версия 0.9.6



### Изменения 0.9.6
- исправлена логика добавления аудиокниг в Избранное: `profile/library_cards` получает UUID связанной книжной карточки, найденной через переключатель **«Текст / Аудио»** на странице произведения; UUID аудиоресурса больше не отправляется как `book_uuid`;
- наличие текстовой и аудиоверсии больше не определяется простым совпадением названия/автора в поиске: для видимых карточек выполняется фоновая проверка реальной карточки Яндекс Книг;
- ложные пары похожих изданий больше не объединяются в значки «текст + аудио» только по совпадению названия;
- проверка версий выполняется лениво и не более трёх запросов одновременно;
- на экране произведения автор и чтец разделены отдельными подписями **«Автор»** и **«Чтец»**;
- на Главной значки текста/аудио стоят непосредственно перед именем автора; такое же компактное отображение используется в Поиске и Избранном.

### Изменения 0.9.5
- исправлена двойная полоска загрузки в Избранном: при пустом списке и первичной загрузке рисуется только один индикатор, а pull-to-refresh имеет отдельное состояние;
- сердечко открытой книги перенесено из области обложки в верхнюю панель рядом с настройками;
- на Главной, в результатах поиска, внутри подборок и в Избранном добавлены компактные значки формата.

### Изменения 0.9.4
- восстановлены прямоугольные обложки на экране книги: обложка больше не обрезается до квадрата, а сердечко остаётся поверх неё в правом нижнем углу;
- кнопки обновления Главной и Избранного убраны из верхней панели; обновление выполняется привычным жестом **потянуть вниз**;
- исправлена двойная индикация загрузки в Избранном: при ручном обновлении используется один pull-to-refresh индикатор, отдельная полоска остаётся только для начальной загрузки/подгрузки следующей страницы;
- в настройках добавлен пункт **«Вкладка при открытии приложения»** с вариантами Главная, Поиск, Избранное и История; выбор сохраняется между запусками.

### Изменения 0.9.3
- на Главной убран дублирующий заголовок **«Главная»**, а обновление подборок перенесено в верхнюю панель рядом с настройками;
- в полном списке подборки, открытом через **«Все»**, добавлены сердечки для добавления/удаления книги из библиотеки;
- кнопка возврата из подборки и с экрана книги перенесена в верхнюю панель слева от названия приложения; системный жест/кнопка «Назад» теперь возвращают на предыдущий экран вместо закрытия приложения;
- на экране поиска кнопка **«Найти»** занимает всю ширину, а ниже расположена отдельная кнопка **«Поиск по ссылке»**;
- устранено визуальное мигание кнопки **«Найти»** во время автоматической подгрузки следующих результатов;
- поле в Избранном переименовано в **«Поиск в избранном»**;
- сердечко на карточке открытой книги перенесено в правый нижний угол обложки;
- в Избранном иконка удаления заменена заполненным сердечком; нажатие сразу снимает книгу с избранного.

### Изменения 0.9.2
- с Главной удалён блок **«Недавно скачанные»** — повторно открывать сохранённые книги можно через отдельную вкладку **«История»**;
- на экране открытой книги добавлено сердечко для добавления/удаления произведения из библиотеки Яндекс Книг;
- из экрана поиска убран дублирующий заголовок **«Поиск»**;
- из Избранного убраны дублирующий заголовок и счётчик загруженных книг; кнопка обновления перенесена в верхнюю панель рядом с настройками;
- из Истории убран дублирующий заголовок; очистка истории перенесена в верхнюю панель и отображается иконкой корзины.

### Исправления 0.9.1
- исправлен поиск по названию и автору: GraphQL-запрос больше не отправляет неподдерживаемый фильтр типов;
- названия разделов на Главной очищены от слитого текста «Все/Всё»;
- рядом с каждым разделом появилась рабочая кнопка **«Все»**, открывающая полный список внутри приложения;
- улучшено получение обложек из `src`, `srcset` и lazy-атрибутов, а первые карточки дополнительно уточняются через API;
- на Главной добавлено отображение авторов;
- карточки книг и недавно скачанных произведений имеют одинаковую фиксированную высоту независимо от длины названия.

### Новая структура приложения
Нижняя навигация теперь состоит из четырёх разделов:

- **Главная** — актуальные подборки Яндекс Книг и популярные поисковые запросы;
- **Поиск** — поиск по каталогу по названию книги или автору, а также отдельная кнопка **«Поиск по ссылке»**;
- **Избранное** — библиотека авторизованного аккаунта с постраничной подгрузкой;
- **История** — сгруппированная история произведений и всех сохранённых форматов.

### Главная
Приложение получает несколько актуальных редакционных разделов с публичной главной страницы `books.yandex.ru` и показывает их горизонтальными подборками. Если структура сайта изменится, поиск, избранное и история продолжат работать, а главная покажет доступные популярные запросы или сообщение о невозможности загрузить подборки.

Дополнительно выводятся популярные поисковые запросы Bookmate/Яндекс Книг. История скачиваний вынесена в отдельную вкладку и на Главной не дублируется.

### Поиск по каталогу
Поиск работает через GraphQL API Bookmate и поддерживает:
- название книги;
- автора;
- текстовые книги;
- аудиокниги;
- автоматическую подгрузку следующей страницы результатов;
- добавление и удаление произведения из библиотеки кнопкой с сердцем как в результатах поиска, так и на экране открытой книги.

Кнопка **«Поиск по ссылке»** открывает прежнее окно ввода ссылки из приложения Яндекс Книги. Пустое поле показывает кнопку вставки из буфера, заполненное — кнопку полной очистки.

### Избранное: бесконечная прокрутка
Ранее приложение могло получить только первую серверную страницу библиотеки, из-за чего на больших аккаунтах отображалась лишь часть книг.

В 0.9.0 библиотека загружается небольшими страницами с `limit`/`offset`. Когда пользователь доходит до конца текущего списка, автоматически запрашивается следующая страница. Это рассчитано в том числе на библиотеки из сотен и тысяч произведений.

Также:
- текстовая и аудиоверсия одного произведения объединяются;
- есть поиск среди уже загруженной части списка;
- при поиске и достижении конца списка новые страницы продолжают подгружаться;
- произведение можно удалить из библиотеки нажатием на заполненное сердечко;
- нажатие на карточку открывает произведение для скачивания.

### Скачивание
Текст:
- EPUB;
- FB2;
- PDF;
- TXT.

Аудио:
- M4A обычного качества;
- M4A максимального качества;
- выбор отдельных глав или всех глав.

Файлы сохраняются в `Загрузки/YBook Downloader/`.

### Поделиться
Кнопка «Поделиться» работает только с уже сохранёнными локальными файлами и не скачивает контент повторно. Для одной аудиокниги несколько отдельных загрузок глав объединяются в один набор сохранённых дорожек.

### Настройки
В настройках доступны режимы оформления:
- **Как в системе**;
- **Светлая**;
- **Тёмная**.

Также можно выбрать вкладку, которая будет открываться по умолчанию при следующем запуске приложения: **Главная**, **Поиск**, **Избранное** или **История**.

## Поддерживаемые версии Android

Android 8.0+ (API 26).

На Android 10+ сохранение в Downloads выполняется через MediaStore без запроса полного доступа к файловой системе. На Android 8/9 приложение запрашивает `WRITE_EXTERNAL_STORAGE` только при сохранении.

## Сборка

GitHub Actions workflow в `.github/workflows/build.yml` автоматически собирает debug APK после push в ветку `main`.

Для публикации в Google Play потребуется release Android App Bundle (`.aab`) и постоянная release-подпись. Debug APK из Actions предназначен для тестирования.
